import { BotaoPrimarioDirective } from './botao-primario.directive';

describe('BotaoPrimarioDirective', () => {
  it('should create an instance', () => {
    const directive = new BotaoPrimarioDirective();
    expect(directive).toBeTruthy();
  });
});
